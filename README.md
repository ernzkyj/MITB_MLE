## Dockerized Python/Jupyter Environment
Built a Docker image (Dockerfile + docker-compose) to run a reproducible 
Python 3.12 + JupyterLab environment. Verified by running both a .py 
script and a .ipynb notebook inside the container.

To run: docker-compose up
Access at: http://127.0.0.1:8888
